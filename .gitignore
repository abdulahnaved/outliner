node_modules
.next
out
.DS_Store

# Local env files (never commit)
.env
.env.local
.env.*.local

# Python backend
.venv
__pycache__/
*.pyc
backend/.venv

# Lab self-signed certs (generate with lab/certs/README.md)
lab/certs/localhost.crt
lab/certs/localhost.key

# Backend scan dataset (append-only, may contain sensitive targets)
backend/data/scans.jsonl
backend/data/scans.csv
backend/data/failures.jsonl

# v* scan runs, cleaned datasets, ML exports/results (generated artifacts)
backend/data/scans.*.jsonl
backend/data/scans.*.csv
backend/data/failures.*.jsonl
backend/data/archive/
backend/data/ml/**
# Keep small runtime ML artifacts in git (used by backend inference)
!backend/data/ml/artifacts/
!backend/data/ml/artifacts/**
# Large cleaned datasets (regenerate from pipeline; keep locally)
backend/data/processed/*.jsonl
backend/data/processed/*.csv

# Next.js app SQLite (local auth + saved scans)
data/*.db
data/*.db-*

